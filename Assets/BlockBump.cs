using UnityEngine;
using System.Collections;

//Description: Complete system for interactive blocks(bouncing, coins, breakables, solid,
//Instruction: Add script to a gameobject and then assigning properties

public class BlockBump : MonoBehaviour {
	/*
	public enum BlockType { 
		blockBounce, 
		blockBreakable, 
		blockSolid, 
		blockQuestion 
	}; // set of labels for blocks for the inspector
	
	public enum PickUpType { 
		pickupHP, 
		pickupMP
	}; // set of labels
	
	public enum BreakType {
		breakableGeometry,
		breakableParicles
	}; // set of labels
	
	private BlockType BlockState;		// block states for enum
	private BlockType BlockStateAfter;	// block after states for enum
	private PickUpType PickUpState;		// pickup states for enum
	private BreakType BreakState;		// breakable state for enum
	
	private float blockQuestionScrollSpeed = 0.5f;	// scroll speed for blockQuestion state
	private Material materialBlock1;	// regular brick block
	private Material materialBlock2;	// solid block
	private Material materialBlock3;	// piece block
	private Material materialBlock4;	// question block
	
	private Transform pickupHPPotion;
	private Transform pickupMPPotion;
	*/
	
	void Start () {
	
	}

	void Update () {
	
	}
}
